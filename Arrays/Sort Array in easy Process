#include <iostream>
#include <algorithm>   // Include this header file.
using namespace std;
int main()
{
    int n=5;
    int a[n]={5,3,2,4,1};
    sort(a,a+n);            // sort(array,array+size); 
    for(int i=0;i<n;i++)
    {
        cout<<a[i]<<" ";
    }
   
    
    return 0;
}
